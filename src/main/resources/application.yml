spring:
  application:
   name: FlightApp

  profiles:
    active: prod  # Change to 'prod' in production or override via command line/environment variable

  # Other global configurations can also go here
  logging:
    level:
      root: INFO
server:
  port: 8000
springdoc:
  swagger-ui:
    path: /docs

